//Arrays are multiple data cells which are all connected

//Declaring "int a[10] ;" or "double b[5] ;" or "char c[8] ;"

// int a[10], num, i; *ACCEPTABLE FORMAT*

// Accessing a single element of an Array; for example: int a[10] = _0 _1 _2 _3 _4 _5 _6 _7 _8 _9 (numbers being used are just position number); use same format "a[0] = 100" just dont use data type 
// POSITION ELEMENT START FROM 0,...,(n-1)

// int a[5] = {1, 2, 3, 4, 5} --gives-- {a-1, a-2, a-3, a-4, a-5}

// int a[5] = {1, 2, 3} --gives-- {a-1, a-2, a-3, a-0, a-0}

// int a[] = {1, 2, 3, 4, 5} --gives-- int a[5], the computer will decide the size so its

#include<iostream>

using namespace std ;

int main () 
{



}